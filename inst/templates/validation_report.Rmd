---
title: {{title}}
author: {{username}}
output: 
  pdf_document:
    toc: true
vignette: >
  %\VignetteIndexEntry{scharpnabValidation Report}
  \usepackage[utf8]{inputenc}
  %\VignetteEngine{knitr::rmarkdown_notangle}
header-includes:
  - \usepackage[dvipsnames, table]{xcolor}
  - \usepackage{float}
---

```{r, setup, echo=FALSE,warning=FALSE}
suppressPackageStartupMessages({
  library(valtools)
  library(knitr)
  library(kableExtra)
  library(magrittr)
  library(devtools)
  library({{pkg_name}})
})

opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  eval = TRUE,
  echo = FALSE,
  results = "asis",
  message = FALSE,
  tidy = FALSE
)

options(knitr.kable.NA = '')

```

```{r shared-obj}
## TODO: verify that this is appropriate for validate outside of package model
##       needs test case
config_file_path <- dirname(vt_find_config())
all_sig <- vt_scrape_sig_table() 
```
\newpage

# Certifications

## Signatures

**AUTHOR** My signature designates authorship of this document.

```{r validation-lead-sig-block}
all_sig[tolower(all_sig$role) == "validation lead",] %>%
  set_rownames(seq_len(nrow(.))) %>%
  vt_kable_sig_table(format = "latex")
```

**APPROVAL** I have reviewed this document and approve its content.

```{r other-roles-sig-block}
all_sig[tolower(all_sig$role) != "validation lead",] %>%
  set_rownames(seq_len(nrow(.))) %>% 
  vt_kable_sig_table(format = "latex")
```

The effective date of this document is the last date of signature.

\newpage

# Release details

## Package Information

### Change Log

```{r news-tab}
vt_scrape_change_log() %>% 
  vt_kable_change_log()
```

### Validation Environment

```{r env-tab}
vt_scrape_val_env() %>% 
  vt_kable_val_env()
```

## Function Authors
### Function Code

```{r func-authors}


# include all function details
# vt_scrape_functions(tags = c("editor", "editDate")) %>% 
#   vt_kable_functions()

# include only exported function details
all_func_details <- vt_scrape_functions()
all_func_details[!is.na(all_function_details$export), c("functions", "editor", "editDate")] %>% 
  vt_kable_functions()
  
```
